version: '3.8'

services:
  app:
    build: .
    container_name: openhands-chat
    ports:
      - "80:12000"
    volumes:
      - ./data:/app/data
      - ./projects:/app/projects
      - ../../:/workspace/project/test03:rw  # Montar raíz del repo test03 (rw para poder editar)
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
