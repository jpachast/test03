/**
 * mcts_optimizer Tool Module
 */
(function() {
    'use strict';
    
    function openmcts_optimizer() {
        var html = '<div style="padding: 20px;"><p style="color: #9ca3af;">Cargando mcts_optimizer...</p><div id="mcts_optimizer-output" style="margin-top: 15px; background: #1f2937; padding: 15px; border-radius: 8px;"></div></div>';
        if (window.showModal) window.showModal('mcts_optimizer', html);
    }
    
    window.openmcts_optimizer = openmcts_optimizer;
})();
